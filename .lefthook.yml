pre-commit:
  parallel: false
  commands:
    format:
      run: bun run format

commit-msg:
  commands:
    cog:
      run: cog verify --file {1}

pre-push:
  parallel: false
  commands:
    lint:
      run: bun run lint
    typecheck:
      run: bun run typecheck
