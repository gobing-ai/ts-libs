---
schema_version: 1
id: "L"
name: "hardening-review-remediation"
status: done
priority: P2
tags: []
created_at: "2026-09-24T03:33:47.295Z"
updated_at: "2026-09-24T05:11:39.804Z"
---

# L: hardening-review-remediation

## Goal

## Scope

- In:
- Out:

## Acceptance Criteria

```gherkin
Feature: 0086 review advisories and MAY deferrals are burned down

  Scenario: AC1 — Non-array shell args fail closed (req: R1)
    When a shell action carries a defined non-array args value
    Then a validation error is raised and nothing executes

  Scenario: AC2 — Reserved placeholder namespace is collision-proof (req: R2)
    When authored command text contains a reserved ${__WF_n} literal without prior binding
    Then a validation error is raised; re-resolution of bound options stays idempotent

  Scenario: AC3 — Containment fails closed without realPath (req: R3)
    When the injected FileSystem lacks realPath
    Then fixer containment refuses the fix instead of degrading silently

  Scenario: AC4 — Extended secret keys redact, analytics keys survive (req: R4)
    When redactValue processes extended secret key names and usage-analytics keys
    Then secrets are redacted and analytics keys are untouched

  Scenario: AC5 — Guards resolve env identically to actions (req: R5)
    When a guard references ${env.X}
    Then it resolves against the run env exactly as actions do

  Scenario: AC6 — Shell guard with args: [] is env-bound end to end (req: R6)
    When a shell guard runs with args: [] and a metachar var
    Then the value rides env binding and no injection executes

  Scenario: AC7 — Sync git context off the deprecated executor (req: R7)
    When getGitContextSync runs with its default executor
    Then the default is NodeSyncProcessExecutor, not BunSyncProcessExecutor

  Scenario: AC8 — Process-group block extracted as a pure move (req: R8)
    When the containment block lives in process-group.ts
    Then all pre-existing runtime tests pass unmodified and the public API is unchanged

  Scenario: AC9 — Gates green (req: R9)
    When "bun run spur-check" and "bun run build" run
    Then both exit 0 and the CHANGELOG Fixed section lists the changes
```

## Tasks

<!-- AUTO-GENERATED by spur feature refresh -->
| WBS | Task | Status |
| --- | ---- | ------ |
| 0086 | Fix 2026-09-23 packages SECUA and architecture review findings | done |
| 0087 | Burn down 0086 review advisories and MAY deferrals (R18/R19) | done |
<!-- END AUTO-GENERATED -->

## Notes

## History

- 2026-09-24T05:09:18.107Z backlog → active (system)
- 2026-09-24T05:09:18.269Z active → verifying (system)
- 2026-09-24T05:11:39.804Z verifying → done (system)

