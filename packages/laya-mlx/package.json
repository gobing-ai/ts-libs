{
    "name": "@gobing-ai/ts-laya-mlx",
    "version": "0.5.2",
    "description": "@gobing-ai/ts-laya-mlx — Local Laya decision backend driving the vendored MLX runtime over a process bridge.",
    "keywords": [
        "typescript",
        "ai",
        "decision",
        "mlx",
        "laya",
        "local"
    ],
    "repository": {
        "type": "git",
        "url": "git+https://github.com/gobing-ai/ts-libs.git",
        "directory": "packages/laya-mlx"
    },
    "author": "Robin Min <minlongbing@gmail.com>",
    "contributors": [
        "Robin Min <minlongbing@gmail.com>"
    ],
    "type": "module",
    "private": false,
    "sideEffects": false,
    "license": "Apache-2.0",
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "exports": {
        ".": {
            "types": "./dist/index.d.ts",
            "import": "./dist/index.js"
        }
    },
    "files": [
        "dist",
        "src",
        "worker",
        "README.md",
        "LICENSE",
        "NOTICE"
    ],
    "scripts": {
        "build": "tsc -p tsconfig.build.json && bun ../../scripts/builder.ts fix-dist-esm-extensions dist",
        "test": "NODE_ENV=test bun test --coverage --coverage-dir=.coverage --reporter=dots",
        "test:full": "NODE_ENV=test bun test --update-snapshots --coverage --coverage-dir=.coverage",
        "typecheck": "tsc --noEmit",
        "lint": "biome check . && bun run typecheck",
        "format": "biome check . --write",
        "parity": "bun scripts/full-parity.ts",
        "check": "bun run lint && bun run test",
        "prepublishOnly": "bun run build",
        "release": "echo 'Manual publish is disabled. Releases go through GitHub Actions via Trusted Publishing — push a tag: git tag @gobing-ai/ts-laya-mlx-v<version> && git push --tags' && exit 1"
    },
    "dependencies": {
        "@gobing-ai/ts-ai-runner": "workspace:*",
        "@gobing-ai/ts-runtime": "workspace:*"
    },
    "devDependencies": {
        "@types/bun": "1.3.14"
    },
    "publishConfig": {
        "access": "public"
    }
}
