# Spur project configuration for ts-libs
# Generated by `spur init`

project:
  name: ts-libs

defaults:
  agent: pi

rules:
  enabled: true
  paths:
    - .spur/rules/**/*.yaml

workflows:
  paths:
    - .spur/workflows/

redaction:
  enabled: false

storage:
  dataDir: data
